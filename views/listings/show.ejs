<% layout("/layouts/boilerplate") %>

<body>
    <div class="row">
        <div class="col-8 offset-3">
    <h3>we have variety of vehicles available </h3>
        </div>
    <div class="card col-4 offset-3">
        <img src="<%= listing.image %>" class="card-img-top show-img" alt="vehicle_image">
        <div class="card-body">
          <p class="card-text">
            <b><%= listing.title %></b><br>
            <%= listing.description %> <br>
            &#8377; <%= listing.price.toLocaleString("en-IN") %> <br>
            <%= listing.location %> <br>
            <%= listing.country %> <br>
        </p>
        </div>
      </div>
    
     
    </form>

    <div class="btns">
        <a href="/listings/<%= listing._id %>/edit" class="btn btn-dark col-1 offset-3">Edit</a>
        <form method="POST" action="/listings/<%= listing._id %>?_method=DELETE">
       <button class="btn btn-dark offset-6" style="background-color: rgb(218, 89, 89);">Delete</button>
       </form>
    </div>
</div>
</body>
